import { TemplateWrapper } from '@/wrappers/TemplateWrapper';

export default function SubscriptionsTemplate({ children }: { children: React.ReactNode }) {
  return <TemplateWrapper>{children}</TemplateWrapper>;
}
